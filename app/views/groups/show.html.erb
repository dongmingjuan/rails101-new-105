<div class="col-md-12">
  <div class="group">
    <span class="pull-right">
      <% if current_user && current_user.is_member_of?(@group) %>
        <label class="label-success">会员</label>
        <%= link_to("Quit Group", quit_group_path(@group), method: :post, class: "btn btn-default")%>
      <% else%>
        <label class="label-warning">不是会员</label>
        <%= link_to("Join Group", join_group_path(@group), method: :post, class: "btn btn-default")%>
      <% end %>
    </span>
   <%= link_to("Write a post", new_group_post_path(@group), class: "btn btn-default pull-right")%>
   <% if current_user && current_user == @group.user %>
    <%= link_to("Edit", edit_group_path(@group), class: "btn btn-primary pull-right")%>
   <% end %>

  </div>
  <h2><%= @group.title %></h2>
  <p>
    <%= render_group_description(@group) %>
  </p>
  <table class="table">
    <thead>
      <tr>
        <td>#</td>
        <td>Content</td>
        <td>Creator</td>
        <td>Time</td>
      </tr>
    </thead>
    <tbody>
      <%= render :partial => "post", :collection => @posts, :as => :post %>
    </tbody>
  </table>
</div>
